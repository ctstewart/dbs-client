const expect = require('chai').expect

// import { mount } from '@vue/test-utils'
// import LayoutOption from '../src/components/layout/LayoutOption'

// // Mount the component and you have the wrapper
// // const wrapper = mount(LayoutOption)

// decribe('LayoutOption', () => {
//     const wrapper = mount(LayoutOption)

//     it('renders the correct markup', () => {
//         expect(wrapper.html()).toContain('Add Phones')
//     })

//     it('has a button', () => {
//         expect(wrapper.contains('button')).toBe(true)
//     })
// })

// // You can access the actual Vue instance via 'wrapper.vm'
// const vm = wrapper.vm

// // To inspect the wrapper deeper just log it to the console
// console.log(wrapper)